# Test properties — override application.properties khi chạy Integration Test
# WireMock khởi động ở port ngẫu nhiên, Spring expose qua wiremock.server.port
# Feign client lấy URL từ đây → gọi WireMock thay vì Inventory Service thật
inventory.url=http://localhost:${wiremock.server.port}
